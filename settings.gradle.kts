pluginManagement {
    repositories {
        google()
        mavenCentral()
        // ... other repositories
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        // ... other repositories
    }
}
rootProject.name="binubaidstore"
include(":app")