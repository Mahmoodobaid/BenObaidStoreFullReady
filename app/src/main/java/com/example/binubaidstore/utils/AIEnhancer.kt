package com.example.binubaidstore.utils


import android.graphics.Bitmap

object AIEnhancer {
    fun enhanceImage(bitmap: Bitmap): Bitmap {
        // تحسين الصورة هنا (في الإصدار الحالي نعيد نفس الصورة للتبسيط)
        return bitmap
    }
}